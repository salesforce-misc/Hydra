= Building from Source
:description: Hydra is a standard Gradle project with its own wrapper — no local Gradle install needed.

This is a standard Gradle project with its own wrapper — no local Gradle install needed.

[source,bash]
----
./gradlew build          # full build incl. tests
./gradlew test           # unit tests
./gradlew spotlessApply  # fix code formatting
----

[cols="1,3",options="header"]
|===
| Command | What it does
| `./gradlew build` | Full build, including tests.
| `./gradlew assemble` | Build without tests.
| `./gradlew test` | Run unit tests.
| `./gradlew test --tests "com.salesforce.hydra.HydraTest"` | Run a specific test class.
| `./gradlew spotlessApply` | Fix code formatting to match the style guide.
|===

== Development environment

* *JDK*: 21+ required for the JVM target.
* *Target*: JVM.

See link:{gh-blob}/DEVELOPMENT.md[DEVELOPMENT.md] for the full development guide and xref:contributing.adoc[Contributing] for setup, formatting, versioning, and publishing.
